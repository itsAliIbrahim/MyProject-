# MyProject

This repository includes a minimal static starter and a Docker environment for Alloy sessions.

## Run locally

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

Open `http://localhost:3000`. The container health check is available at `http://localhost:3000/health`.
